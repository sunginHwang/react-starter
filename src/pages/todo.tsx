import React from 'react';
import TodoContainer from "../containers/TodoContainer";

type todoProps = {}

function Todo({}: todoProps) {

    return (
        <>
            <TodoContainer/>
        </>
    );
};

export default Todo;
